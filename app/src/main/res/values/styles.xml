<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="actionMenuTextColor">@color/color_accent</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">@color/color_primary</item>
        <item name="android:fontFamily">@font/museo</item>
        <item name="titleTextStyle">@style/MyActionBarTitleText</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText"
        parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">@color/color_accent</item>
        <item name="android:fontFamily">@font/museo</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>

    <style name="Numpad">
        <item name="android:gravity">center</item>
        <item name="android:fontFamily">@font/montserrat</item>
    </style>

    <style name="Numpad.WhiteButtons">
        <item name="android:textSize">29sp</item>
        <item name="android:textColor">@color/color_accent</item>
        <item name="android:background">@drawable/number_button</item>
    </style>

    <style name="Numpad.BlueButtons">
        <item name="android:textSize">29sp</item>
        <item name="android:textColor">@color/color_primary</item>
        <item name="android:background">@drawable/operation_button</item>
    </style>
</resources>
